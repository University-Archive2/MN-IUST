\Question{
	\lr{UMTS}
}{
	پارامترهای توانی در شبکه‌های
	\lr{UMTS}
	\SubQuestion{\lr{RSCP}}
	{
		پارامتر 
		\lr{RSCP}\LTRfootnote{Received Signal Code Power}
		در شبکه‌های
		\lr{UMTS}
		نشان‌دهنده‌ی توان اندازه‌گیری شده‌ی دریافتی در یک کانال ارتباطی است و واحد آن 
		\lr{dBm}
		است. این پارامتر توسط 
		\lr{UE}
		اندازه‌گیری می‌شود و به 
		\lr{NodeB}
		گزارش داده می‌شود. باید توجه کنیم که این پارامتر فقط در نسل ۳ موجود است.
	}
	\SubQuestion{\lr{Ec/No}}
	{
		این پارامتر برای اندازه‌گیری کیفیت توان دریافتی است و مقدار از تقسیم
		\lr{RSCP}
		بر مقدار
		\lr{RSSI}
		به دست می‌آید.
	}
	\SubQuestion{\lr{RSSI}}
	{
		پارامتر 
		\lr{RSSI}
		در شبکه‌های
		\lr{UMTS}
		همان تعریفی را که در شبکه‌های نسل قبل داشت، دارد.
	}
}